# Dotfiles

## Tracked files:
* `.i3/config`
* `.tmux.conf`
* `.vimrc`
* `.Xresources`
* `.nethackrc`
* `.config/termite/config`
* `.zshrc`
* `.dircolors`
* `install.py`


# Usage:
Use `install.py` to create symlinks to dotfiles in your home directory.

