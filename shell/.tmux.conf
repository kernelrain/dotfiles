# Use C-a instead of C-b as prefix
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# window splitting
bind s split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

# vim navigation keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# resize
bind  > resize-pane -R 10
bind  < resize-pane -L 10
bind  - resize-pane -D 10
bind  + resize-pane -U 10

set -g default-terminal "screen-256color"
setw -g mode-keys vi

# enable mouse
set -g mouse on

# notify on activity
set-window-option -g monitor-activity on

# disable esc delay
set -s escape-time 0

# copy to system clipboard
bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "xclip -i -f -selection primary | xclip -i -selection clipboard"

# reload
bind r source-file ~/.tmux.conf \; display "Reloaded configuration!"


# set-option -g status-bg black
# set-option -g status-fg blue
# set-option -g status-interval 5
# # set-option -g visual-activity on
# set-window-option -g monitor-activity on
# set-window-option -g window-status-current-fg white
# set -g status-justify "left"

# set -g status "on"
# set -g status-left-style "none"
# set -g message-command-style "fg=colour144,bg=colour237"
# set -g status-right-style "none"
# set -g pane-active-border-style "fg=colour27"
# # set -g status-utf8 "on"
# set -g status-style "bg=colour234,none"
# set -g message-style "fg=colour144,bg=colour237"
# set -g pane-border-style "fg=colour237"
# set -g status-right-length "100"
# set -g status-left-length "100"
# setw -g window-status-activity-attr "none"
# setw -g window-status-activity-style "fg=colour27,bg=colour234,none"
# setw -g window-status-separator ""
# setw -g window-status-style "fg=colour39,bg=colour234,none"
# set -g status-left "#[fg=colour15,bg=colour27,bold] #S #[fg=colour27,bg=colour234,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=colour237,bg=colour234,nobold,nounderscore,noitalics]#[fg=colour144,bg=colour237] %d.%m.%Y %H:%M #[fg=colour27,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour15,bg=colour27,bold] #h "
# setw -g window-status-format "#[fg=colour39,bg=colour234] #I #[fg=colour39,bg=colour234] #W "
# setw -g window-status-current-format "#[fg=colour234,bg=colour237,nobold,nounderscore,noitalics]#[fg=colour144,bg=colour237] #I #[fg=colour144,bg=colour237] #W #[fg=colour237,bg=colour234,nobold,nounderscore,noitalics]"
#
